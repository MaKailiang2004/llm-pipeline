CORE_CONF_fs_defaultFS=hdfs://namenode:9000
HDFS_CONF_dfs_replication=1
# DataNode 对外公布地址：使用宿主机 IP 使远端 Ray Worker 能连接 DataNode 读取块数据。
# 通过 docker-compose 的 HDFS_CONF_dfs_datanode_hostname 注入（见 HDFS_DATANODE_ANNOUNCE_HOST）。
